{
  "name": "qa-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:report": "playwright test --reporter=html && npx playwright show-report",
    "prepare": "echo 'husky install (optional)'"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "dotenv": "^16.6.1",
    "typescript": "^5.6.2"
  }
}
